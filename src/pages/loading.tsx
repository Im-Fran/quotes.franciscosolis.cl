import {CircleDashedIcon} from "lucide-react";

const Loading = () => <div className={"flex items-center justify-center w-full min-h-screen"}>
  <CircleDashedIcon size={128} className={"animate-spin"}/>
</div>

export default Loading